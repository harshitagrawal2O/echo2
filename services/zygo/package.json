{
  "name": "cyanbridge-zygo-service",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Server-side Zygo (UPI-over-Solana) endpoints for CyanBridge. Holds the merchant API key so the Android app never does.",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "dev": "vercel dev",
    "check": "node --check lib/zygo.js && node --check lib/http.js && for f in api/*.js; do node --check \"$f\" || exit 1; done"
  },
  "dependencies": {
    "@zygopay/sdk": "^0.1.4",
    "@solana/web3.js": "^1.95.0",
    "@solana/spl-token": "^0.4.8",
    "@coral-xyz/anchor": "^0.30.1"
  }
}
